type Query {
  user(id: ID!): User!
  users(name: String, limit: Int, after: String): [User!]
  company(id: ID!): Company!
  companies(name: String, limit: Int, after: String): [Company!]
}

type Mutation {
  updateUser(user: UserInput!): Boolean
}

schema {
  query: Query
  mutation: Mutation
}
